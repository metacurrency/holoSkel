version: '3'

services:
  service.chain:
    build:
      context: .
      dockerfile: Dockerfile.seedService
    environment:
      - LOCAL_USER_ID
    volumes:
      - seed:/chain.seeded
      - ./Scripts:/Scripts
    command: /Scripts/cluster/pre.up
  bs:
    build:
      context: .
      dockerfile: Dockerfile.bsService
    ports:
      - "10000"
    environment:
      - LOCAL_USER_ID
    volumes:
      - seed:/chain.seeded
      - ./Scripts:/Scripts
    command: /Scripts/cluster/bs.sync
    #command: /bin/sh
    depends_on: [service.chain]
  hc:
    build:
      context: .
      dockerfile: Dockerfile.serveInstance
    ports:
      - "3141"
      - "6283"
    environment:
      - LOCAL_USER_ID
    volumes:
      - seed:/chain.seeded
      - ./Scripts:/Scripts
    command: /Scripts/cluster/hc.sync
    #command: /bin/sh
    depends_on: [bs]
  hc.post.up:
    build:
      context: .
      dockerfile: Dockerfile.hc.post.up
    environment:
      - LOCAL_USER_ID
    volumes:
      - seed:/chain.seeded
      - ./Scripts:/Scripts
    command: /Scripts/cluster/post.up
    depends_on: [hc]

volumes:
  seed: 
