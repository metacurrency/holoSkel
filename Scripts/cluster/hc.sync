#!/bin/sh
echo #!/bin/sh $0

seedID=`cat /chain.seeded/.seedID`
clusterDir="/chain.seeded/cluster/$seedID"

echo "Seed host name: $seedID"
echo "My host name: $HOSTNAME"

mkdir -p $clusterDir/hc
touch $clusterDir/hc/$HOSTNAME
