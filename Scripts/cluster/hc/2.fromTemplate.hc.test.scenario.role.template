#!/bin/sh
export scenario="backnforth"
export role="person2"


ls /chain.seeded -la
seedID=`cat /chain.seeded/devchain/.seedID`

echo "hc init $HOSTNAME"
hc init $HOSTNAME
echo

echo "hc join /.holochain/devchain"
hc join /chain.seeded/devchain devchain
echo
echo "hc test devchain $scenario $role $seedID"
hc test devchain $scenario $role $seedID
echo

